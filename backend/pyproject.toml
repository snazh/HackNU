[tool.poetry]
name = "backend"
version = "0.1.0"
description = "Package for Hackaton"
authors = ["Sanzhar <sanxius.kon@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
fastapi = {extras = ["standard"], version = "^0.119.0"}
pydantic-settings = "^2.11.0"
alembic = "^1.17.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
